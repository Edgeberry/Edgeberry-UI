import { Alert, Container } from "react-bootstrap";

const Application = ()=>{
    return(
        <Container>
            <h2>Application</h2>
            <Alert>Here comes everything related to the device's application</Alert>
        </Container>
    );
}

export default Application;