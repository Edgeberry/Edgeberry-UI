import { Alert, Container } from "react-bootstrap";

const System = ()=>{
    return(
        <Container>
            <h2>System</h2>
            <Alert>Here comes everything related to device's system</Alert>
        </Container>
    );
}

export default System;