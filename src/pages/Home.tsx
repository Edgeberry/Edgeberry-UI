import { Alert, Container } from "react-bootstrap";

const Home = ()=>{
    return(
        <Container>
            <h1>Edge Gateway</h1>
            <Alert>
                Some usefull first-sight info
            </Alert>
        </Container>
    );
}

export default Home;